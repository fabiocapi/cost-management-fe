import { TipologiaContratto } from './tipologia-contratto';

describe('TipologiaContratto', () => {
  it('should create an instance', () => {
    expect(new TipologiaContratto()).toBeTruthy();
  });
});
